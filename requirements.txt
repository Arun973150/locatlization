# torch/torchvision come from the RunPod PyTorch image — do NOT pip install here
# (use a RunPod template with PyTorch 2.4+ / CUDA 12.x).
transformers>=4.56.0        # DINOv3 support; if AutoModel fails: pip install -U git+https://github.com/huggingface/transformers
huggingface_hub>=0.25.0
datasets>=2.14.0
requests>=2.31.0
peft>=0.13.0
pillow>=10.0.0
opencv-python-headless>=4.8.0
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
scikit-image>=0.22.0
tqdm>=4.66.0
pyyaml>=6.0
einops>=0.7.0
